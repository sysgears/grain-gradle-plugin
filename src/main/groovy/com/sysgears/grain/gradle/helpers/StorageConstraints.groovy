package com.sysgears.grain.gradle.helpers

/**
 * Theme storage constraints.
 */
class StorageConstraints {

    /** GitHub username of the user or the organization. */
    static final String GITHUB_USERNAME = 'sysgears'

    /** Prefix for the theme repositories. */
    static final String THEME_REPO_PREFIX = 'grain-theme-'

    /** Prefix for the version tags. */
    static final String VERSION_TAG_PREFIX = 'v'
}
